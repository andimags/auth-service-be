export enum UserStatusType {
    active = 'active',
    inactive = 'inactive'
}

export enum PermissionScopeType {
    global = 'global',
    channel = 'channel'
}

export enum PermissionAccessLevelType {
    read = 'read',
    write = 'write',
    admin = 'admin'
}
